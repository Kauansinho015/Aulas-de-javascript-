console.log('parte1')

//Estruturas de repetiçao

//tipos
//for "normal
//repetiçao por Array

//Estrutura do forEach

//nomedoArray.forEach(nome da variavel const =>{
  
//})

//for normal:

const cidade = ['ipaussu','chavantes','canitar']

cidade.forEach(elemento =>{
  
  console.log(elemento);
  
})


//for normal tambem

const cores = ['azul', 'verde', 'preto'];

cores.forEach(elemento => {
  console.log(elemento);
});
